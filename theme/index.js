/**
 * Theme Configuration for @build-stack/md3-tailwind
 * Similar to Material Tailwind's theme structure
 */

const { buildStackTheme } = require('../packages/react/src/theme/tokens');

module.exports = buildStackTheme;